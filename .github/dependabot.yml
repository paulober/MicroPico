version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    # give new releases a week before adopting them (security updates skip this)
    cooldown:
      default-days: 7
    open-pull-requests-limit: 5
    groups:
      dev-dependencies:
        dependency-type: development
        update-types: [minor, patch]
      dependencies:
        dependency-type: production
        update-types: [minor, patch]
    ignore:
      # tied to engines.vscode and the Node.js version VS Code ships
      - dependency-name: "@types/vscode"
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
      # breaks the bundled undici at runtime, fixed upstream but unreleased
      # (webpack/webpack#22022); drop once a fixed version is out
      - dependency-name: "webpack"
        versions: ["5.110.0", "5.110.1", "5.110.2", "5.110.3"]

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    cooldown:
      default-days: 7
    groups:
      actions:
        patterns: ["*"]
